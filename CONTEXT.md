# h3x 领域术语

本词汇表记录公共 API 评审中已明确的领域含义；现有实现可能仍使用旧模型。

## Language

**Endpoint**：
代表本地身份的对象，包含名称及其身份材料。
_Avoid_: 用 Endpoint 指代身份与网络运行时的组合。

**Message<B>**：
请求与响应共用的消息内容，包含消息首部与 Body；B 区分 Fixed 与 Chunk。

**Fixed**：
完整内容已在内存中的消息 Body。
_Avoid_: 将对端声明 Content-Length 等同于 Fixed。

**Chunk**：
可逐段生产或消费的消息 Body；总长度可以已知，也可以未知。

**交换**：
一次 HTTP 请求及其对应响应组成的交互，包含相互独立的上传与响应读取方向。

**整体取消**：
显式终止一次交换的上传与响应读取两方向。
_Avoid_: 将丢弃响应 Body、停止单方向或转移消息外壳称为整体取消。
